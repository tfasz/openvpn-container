version: '3.3'

services:
  openvpn:
    container_name: openvpn
    image: tfasz/openvpn
    cap_add:
      - NET_ADMIN
    volumes:
      - ./data:/etc/openvpn
    ports:
      - "1194:1194"
      - "1194:1194/udp"